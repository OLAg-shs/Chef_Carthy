# Chef_Carthy OS - ZRAM In-Memory Compression Generator
# Provides fast compressed RAM swap using ZSTD with zero disk wear

[zram0]
zram-size = ram / 2
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
